# LOCAL DEVELOPMENT ENVIRONMENT for: Scoring Engine Service
# INSTRUCTIONS: Copy this file to ".env" -> `cp .env.example .env`

# --- Application Settings ---
ENVIRONMENT=development
LOG_LEVEL=DEBUG

# --- Databases (match ports from your docker-compose.dev.yml) ---
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=scoring_metadata_db

CLICKHOUSE_HOST=localhost
CLICKHOUSE_PORT=8123
CLICKHOUSE_USER=default
CLICKHOUSE_PASSWORD=

# --- Redis (for Cache & Pub/Sub notifications) ---
REDIS_HOST=localhost
REDIS_PORT=6379

# --- Message Broker (to consume signals from Data Pipeline) ---
# For local dev, we often use the same Redis instance.
MESSAGE_BROKER_URL=redis://localhost:6379/0